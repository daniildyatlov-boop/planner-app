# Руководство по участию в разработке

Спасибо за интерес к проекту Планер! Это руководство поможет вам внести свой вклад.

## 🚀 Быстрый старт

### Настройка окружения
```bash
# Клонирование репозитория
git clone https://github.com/your-username/planner.git
cd planner

# Запуск локального сервера
python3 -m http.server 8000
# или
npx serve .
```

### Тестирование в Telegram
```bash
# Установка ngrok
npm install -g ngrok

# Запуск туннеля
ngrok http 8000

# Используйте HTTPS URL в BotFather
```

## 📁 Структура проекта

```
├── app.html              # Полная версия (v6.18+)
├── index.html            # Базовая версия (v5.1+)
├── fast-planner.html     # Быстрая версия (v5.1+)
├── manifest.json         # PWA манифест
├── test-storage.html     # Тестирование localStorage
├── README.md            # Основная документация
├── CHANGELOG.md         # История изменений
└── CONTRIBUTING.md      # Это руководство
```

## 🔧 Архитектура

### localStorage ключи
- `planner_plans` - массив всех планов
- `planner_teams` - массив комнат/команд
- `planner_userName` - имя пользователя

### Основные функции
```javascript
// Загрузка данных
loadPlans() / loadData()

// Сохранение
savePlans() / saveRooms()

// Рендеринг
render()

// Фильтрация по дате
getPlansForDate(date)
```

## 🐛 Отладка

### Проверка данных
```javascript
// В консоли браузера
console.log('Plans:', JSON.parse(localStorage.getItem('planner_plans') || '[]'));
console.log('Teams:', JSON.parse(localStorage.getItem('planner_teams') || '[]'));
```

### Тестовые данные
Откройте `test-storage.html` для добавления тестовых данных

### Очистка данных
```javascript
localStorage.clear(); // Полная очистка
location.reload();    // Перезагрузка
```

## 📝 Стандарты кода

### HTML
- Используйте семантические теги
- Минимизируйте inline стили
- Добавляйте комментарии для сложных секций

### CSS
- Используйте CSS переменные для цветов
- Группируйте стили по компонентам
- Добавляйте комментарии для сложных анимаций

### JavaScript
- Используйте `const`/`let` вместо `var`
- Добавляйте JSDoc комментарии для функций
- Обрабатывайте ошибки в try/catch блоках

### Пример функции
```javascript
/**
 * Сохраняет планы в localStorage
 * @param {Array} plans - массив планов для сохранения
 * @returns {boolean} успешность операции
 */
function savePlans(plans) {
  try {
    localStorage.setItem('planner_plans', JSON.stringify(plans));
    return true;
  } catch (e) {
    console.error('Error saving plans:', e);
    return false;
  }
}
```

## 🔄 Процесс разработки

### 1. Создание Issue
- Опишите проблему или предложение
- Добавьте метки (bug, enhancement, etc.)
- Приложите скриншоты если нужно

### 2. Создание ветки
```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

### 3. Разработка
- Делайте небольшие, логичные коммиты
- Тестируйте изменения в браузере и Telegram
- Обновляйте документацию при необходимости

### 4. Тестирование
- Проверьте все версии (app.html, index.html, fast-planner.html)
- Убедитесь в совместимости данных
- Протестируйте на мобильных устройствах

### 5. Pull Request
- Опишите изменения
- Приложите скриншоты/GIF
- Укажите связанные Issues

## 🧪 Тестирование

### Ручное тестирование
1. Создание планов
2. Редактирование планов
3. Удаление планов
4. Переключение дат
5. Работа с комнатами
6. Сохранение при перезагрузке

### Тестирование совместимости
1. Создайте данные в одной версии
2. Переключитесь на другую версию
3. Убедитесь в сохранности данных

### Тестирование миграции
1. Создайте старые данные (ключи 'plans', 'teams')
2. Обновите страницу
3. Проверьте миграцию в новые ключи

## 📋 Чеклист для PR

- [ ] Код протестирован во всех версиях
- [ ] Данные корректно сохраняются/загружаются
- [ ] Нет ошибок в консоли браузера
- [ ] Интерфейс работает на мобильных устройствах
- [ ] Обновлена документация (если нужно)
- [ ] Добавлены комментарии к сложному коду

## 🎯 Приоритетные задачи

### Высокий приоритет
- Исправление критических багов
- Проблемы с сохранением данных
- Ошибки в Telegram Mini App

### Средний приоритет
- Улучшение UX/UI
- Оптимизация производительности
- Новые функции

### Низкий приоритет
- Рефакторинг кода
- Дополнительные анимации
- Экспериментальные функции

## 💡 Идеи для вклада

### Для начинающих
- Исправление опечаток в документации
- Улучшение CSS стилей
- Добавление комментариев к коду

### Для опытных
- Новые функции планирования
- Интеграция с внешними API
- Оптимизация производительности

### Для экспертов
- Архитектурные улучшения
- Система плагинов
- Продвинутая аналитика

## 📞 Связь

- **Issues**: для багов и предложений
- **Discussions**: для общих вопросов
- **Email**: для приватных вопросов

---

Спасибо за ваш вклад в развитие Планера! 🚀