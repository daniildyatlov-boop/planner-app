# МИГРАЦИЯ ДАННЫХ: Очистка рутинных задач из localStorage

## ПРОБЛЕМА
Рутинные задачи уже сохранены в localStorage и продолжают загружаться при старте приложения, несмотря на то что автосоздание убрано из кода.

## РЕШЕНИЕ
Одноразовая миграция данных при загрузке приложения для удаления всех рутинных задач из localStorage.

## РЕАЛИЗАЦИЯ

### 🧹 Миграция в loadData()
```javascript
// 🧹 ОДНОРАЗОВАЯ МИГРАЦИЯ: Удаляем все рутинные задачи из localStorage
const migrationKey = 'planner_migration_routine_cleanup';
if (!localStorage.getItem(migrationKey)) {
  console.log('🧹 МИГРАЦИЯ: Удаляем рутинные задачи из localStorage...');
  
  const initialCount = plans.length;
  
  // Удаляем все рутинные задачи по критериям:
  // 1. sectionId != null (задачи из разделов)
  // 2. createdFromSection === true (флаг рутинной задачи)  
  // 3. ID начинается с "section:" (детерминированный ID рутинных задач)
  for (let i = plans.length - 1; i >= 0; i--) {
    const plan = plans[i];
    const isRoutineTask = plan.sectionId != null || 
                         plan.createdFromSection === true || 
                         (plan.id && plan.id.toString().startsWith('section:'));
    
    if (isRoutineTask) {
      console.log(`🗑️ Удаляем рутинную задачу: "${plan.title}" (ID: ${plan.id})`);
      plans.splice(i, 1);
    }
  }
  
  const removedCount = initialCount - plans.length;
  console.log(`🧹 МИГРАЦИЯ ЗАВЕРШЕНА: удалено ${removedCount} рутинных задач из ${initialCount}`);
  
  // Сохраняем очищенные планы
  if (removedCount > 0) {
    savePlans();
  }
  
  // ДОПОЛНИТЕЛЬНАЯ ОЧИСТКА: Удаляем старые ключи localStorage
  const oldKeys = ['plans', 'planner_rooms']; // Старые ключи
  oldKeys.forEach(key => {
    if (localStorage.getItem(key)) {
      console.log(`🧹 Удаляем старый ключ localStorage: ${key}`);
      localStorage.removeItem(key);
    }
  });
  
  // Устанавливаем флаг что миграция выполнена
  localStorage.setItem(migrationKey, 'true');
  console.log('✅ Флаг миграции установлен - больше не будет повторяться');
}
```

## КРИТЕРИИ УДАЛЕНИЯ РУТИННЫХ ЗАДАЧ

### 1. ✅ По полю sectionId
```javascript
plan.sectionId != null // Задачи связанные с разделами
```

### 2. ✅ По флагу createdFromSection  
```javascript
plan.createdFromSection === true // Флаг автоматического создания
```

### 3. ✅ По детерминированному ID
```javascript
plan.id.toString().startsWith('section:') // ID формата "section:1001:1:2024-01-28"
```

## ДОПОЛНИТЕЛЬНАЯ ОЧИСТКА

### 🧹 Удаление старых ключей localStorage:
- `plans` - старый ключ для планов
- `planner_rooms` - старый ключ для разделов

## ФЛАГ МИГРАЦИИ

### 🔒 Предотвращение повторного выполнения:
```javascript
localStorage['planner_migration_routine_cleanup'] = 'true'
```

Миграция выполняется ТОЛЬКО ОДИН РАЗ при первой загрузке после обновления.

## ПОРЯДОК ВЫПОЛНЕНИЯ

1. **Загрузка планов** из localStorage
2. **Проверка флага миграции** - если уже выполнена, пропускаем
3. **Удаление рутинных задач** по критериям
4. **Сохранение очищенных планов** обратно в localStorage
5. **Удаление старых ключей** localStorage
6. **Установка флага миграции** для предотвращения повтора

## РЕЗУЛЬТАТ

### ✅ После миграции:
- Все рутинные задачи удалены из localStorage
- Ручные задачи сохранены
- Задачи питания сохранены  
- Старые ключи localStorage очищены
- Флаг миграции установлен

### ✅ При последующих загрузках:
- Миграция не выполняется (флаг установлен)
- Рутинные задачи НЕ загружаются
- Автосоздание рутинных задач отключено
- Удаленные задачи НЕ возвращаются

## ПРОВЕРКА РАБОТЫ

### 🧪 Тест миграции:
1. Открыть приложение → проверить консоль на сообщения миграции
2. Reload → миграция НЕ должна повториться
3. Рутинные задачи НЕ должны появляться в списке

### 🧪 Тест сохранения данных:
1. Ручные задачи остаются после миграции
2. Задачи питания остаются после миграции
3. Удаленные задачи НЕ возвращаются

## СТАТУС: ✅ РЕАЛИЗОВАНО

Одноразовая миграция данных добавлена в loadData(). Рутинные задачи будут удалены из localStorage при первой загрузке приложения.