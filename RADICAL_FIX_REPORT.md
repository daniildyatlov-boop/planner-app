# РАДИКАЛЬНОЕ РЕШЕНИЕ: Полная очистка всех данных

## ПРОБЛЕМА
Рутинные задачи продолжают возвращаться несмотря на все предыдущие исправления. Это указывает на глубоко укоренившуюся проблему в данных localStorage.

## РАДИКАЛЬНОЕ РЕШЕНИЕ
Полная очистка всех данных связанных с задачами и разделами при первой загрузке после обновления.

## РЕАЛИЗАЦИЯ

### 🚨 Принудительная миграция:
```javascript
// РАДИКАЛЬНОЕ РЕШЕНИЕ: Принудительно очищаем ВСЕ что связано с рутинными задачами
console.log('🚨 РАДИКАЛЬНАЯ ОЧИСТКА: Удаляем ВСЕ следы рутинных задач');

// 1. Очищаем plans массив полностью
plans.length = 0;

// 2. Удаляем ВСЕ ключи localStorage
const keysToRemove = [
  'planner_plans',
  'planner_sections', 
  'planner_teams',
  'planner_rooms',
  'plans',
  'teams',
  'rooms',
  'sections'
];

keysToRemove.forEach(key => {
  if (localStorage.getItem(key)) {
    localStorage.removeItem(key);
  }
});

// 3. Очищаем массивы в памяти
sections.length = 0;
rooms.length = 0;
deletedTaskIds.length = 0;

// 4. Сохраняем пустые массивы
savePlans(); // Сохранит пустой массив
```

## УДАЛЯЕМЫЕ КЛЮЧИ localStorage

### 📁 Основные ключи:
- `planner_plans` - текущие планы
- `planner_sections` - разделы (источник рутинных задач)
- `planner_teams` - команды/разделы

### 📁 Старые ключи:
- `plans` - старый формат планов
- `sections` - старый формат разделов  
- `teams` - старый формат команд
- `rooms` - старый формат разделов

## ИНСТРУМЕНТЫ ДИАГНОСТИКИ

### 🛠️ debug-storage.html:
- **Проверить localStorage** - показывает все данные
- **Сбросить флаг миграции** - для повторного запуска
- **🚨 ПРИНУДИТЕЛЬНАЯ ОЧИСТКА** - радикальное решение
- **Очистить все** - полная очистка localStorage

## ПОРЯДОК ДЕЙСТВИЙ

### 1. 🔍 Диагностика:
1. Открыть `debug-storage.html`
2. Нажать "Проверить localStorage"
3. Посмотреть есть ли рутинные задачи

### 2. 🚨 Радикальная очистка:
1. Нажать "🚨 ПРИНУДИТЕЛЬНАЯ ОЧИСТКА"
2. Перезагрузить приложение
3. Проверить что рутинных задач нет

### 3. ✅ Проверка результата:
1. Создать ручную задачу → должна работать
2. Удалить задачу → не должна вернуться
3. Reload → задач не должно быть

## РЕЗУЛЬТАТ

### ✅ После радикальной очистки:
- Все рутинные задачи удалены
- Все разделы удалены
- Все старые данные удалены
- Приложение начинает с чистого листа

### ✅ Что будет работать:
- Создание ручных задач
- Создание задач питания
- Удаление задач (навсегда)
- Все кнопки и функции

### ✅ Что НЕ будет работать (правильно):
- Рутинные задачи (не создаются)
- Разделы (удалены)
- Автосоздание задач (отключено)

## СТАТУС: 🚨 РАДИКАЛЬНОЕ РЕШЕНИЕ ГОТОВО

Если проблема остается даже после этого - значит есть скрытый код который создает задачи в runtime, не из localStorage.