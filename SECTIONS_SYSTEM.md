# 🎯 Система разделов с автоматической синхронизацией

## Описание

Реализована система управления рутинными задачами через разделы с правилами активности. Система автоматически создает задачи в главном экране "Сегодня" только в дни, когда разделы активны.

## Архитектура

### Структуры данных

**Разделы (sections)**:
```javascript
{
  id: number,
  name: string,           // "Утренняя зарядка"
  icon: string,           // "🏃"
  activityRule: {
    type: 'daily' | 'every_other_day' | 'weekdays',
    value?: number | number[]  // для weekdays: [1,2,3,4,5]
  },
  templateTasks: [
    {
      id: number,
      title: string,      // "Разминка"
      time: string        // "07:00" или ""
    }
  ],
  createdAt: string
}
```

**Планы (plans)** - дополнены полями:
```javascript
{
  // ... существующие поля
  sectionId?: number,        // ID раздела
  templateId?: number,       // ID шаблонной задачи
  createdFromSection?: boolean  // флаг автоматического создания
}
```

### Правила активности

1. **daily** - каждый день
2. **every_other_day** - через день (используется четность дней с эпохи)
3. **weekdays** - по дням недели (массив: 0=Вс, 1=Пн, ..., 6=Сб)

## Логика работы

### При старте приложения
1. Загружаются разделы из localStorage
2. Вызывается `syncSectionsToPlans(currentDate)`
3. Для каждого раздела проверяется активность на текущую дату
4. Если активен - создаются задачи из шаблонов (без дубликатов)
5. Если неактивен - удаляются автоматически созданные задачи

### При смене даты
- В функциях `changeDay()` и `selectDate()` добавлен вызов синхронизации
- Система автоматически адаптирует список задач под новую дату

### Управление разделами
- Доступно через экран "Разделы" → "Рутинные разделы"
- Создание разделов с настройкой правил активности
- Добавление шаблонных задач с временем
- Удаление разделов (с очисткой связанных задач)

## Ключевые функции

### Основные
- `initSectionsSync()` - инициализация при старте
- `syncSectionsToPlans(date)` - синхронизация на дату
- `isSectionActiveOnDate(section, date)` - проверка активности

### Управление
- `showSectionsManager()` - интерфейс управления
- `showAddSection()` - создание раздела
- `saveNewSection()` - сохранение раздела
- `deleteSection(id)` - удаление раздела

### Хранение
- `saveSections()` / `loadSections()` - работа с localStorage
- Ключ: `planner_sections`

## Демонстрационные разделы

При первом запуске создаются:

1. **🏃 Утренняя зарядка** (каждый день)
   - Разминка (07:00)
   - Упражнения (07:15)
   - Растяжка (07:30)

2. **🧹 Уборка дома** (через день)
   - Пропылесосить (10:00)
   - Протереть пыль (10:30)

3. **💼 Рабочие задачи** (пн-пт)
   - Проверить почту (09:00)
   - Планерка (10:00)
   - Отчет за день (18:00)

## Интеграция

Система полностью интегрирована в существующее приложение:
- Не нарушает работу обычных задач
- Использует существующие функции рендеринга
- Совместима с системой комнат/разделов
- Автоматически мигрирует при обновлении

## Тестирование

Для тестирования создан файл `sections-demo.html` с интерактивной демонстрацией работы системы на разные даты.

## Преимущества

✅ **Автоматизация** - рутинные задачи появляются сами  
✅ **Гибкость** - разные правила активности  
✅ **Чистота** - главный экран не знает о правилах  
✅ **Надежность** - нет дубликатов, автоочистка  
✅ **Простота** - интуитивный интерфейс управления  

Система готова к использованию! 🚀